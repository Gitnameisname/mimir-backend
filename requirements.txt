# ---- 런타임 의존성 ----
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.13.0
click==8.3.2
fastapi==0.135.3
PyJWT==2.10.1
redis==5.2.1
slowapi==0.1.9
h11==0.16.0
httptools==0.7.1
httpx==0.28.1
idna==3.11
openai==1.84.0
pymilvus==2.5.8
psycopg2-binary==2.9.11
pydantic==2.12.5
pydantic-settings==2.13.1
pydantic_core==2.41.5
python-dotenv==1.2.2
PyYAML==6.0.3
starlette==1.0.0
tiktoken==0.9.0
typing-inspection==0.4.2
typing_extensions==4.15.0
uvicorn==0.43.0
uvloop==0.22.1
watchfiles==1.1.1
websockets==16.0

# ---- Phase 14: 인증 시스템 의존성 ----
bcrypt>=4.0.0,<5.0.0
cryptography>=43.0.0,<45.0.0

# ---- S2-5 (2026-04-20): Alembic 기반 DB 마이그레이션 ----
# Alembic 1.13+ 는 SQLAlchemy 2.0+ 가 필요. ORM 은 쓰지 않고 raw SQL (op.execute) 전용.
alembic>=1.13,<2.0
SQLAlchemy>=2.0,<3.0

# ---- 개발/테스트 의존성 ----
pytest==8.3.5
pytest-cov==6.1.0
pytest-asyncio==0.26.0
# httpx는 위 런타임 의존성에 이미 포함 (중복 제거)
bandit==1.8.3
ruff==0.11.5
locust==2.34.0

# ---- S3 Phase 0 / FG 0-1 (2026-04-23): 실 DB 통합 테스트 ----
# testcontainers 는 로컬 개발자가 `pytest tests/integration/` 실행 시 PG/Valkey 컨테이너를 자동 기동하도록 한다.
# CI 는 GitHub Actions `services:` 블록을 사용하므로 testcontainers 를 쓰지 않지만, 두 경로 모두 동일 conftest 를 공유한다.
testcontainers[postgres]>=4.7,<5.0
